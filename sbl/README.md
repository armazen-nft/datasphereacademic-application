# Semantic Bridge Layer (SBL)

This directory contains the initial in-repository scaffold for `sbl-semantic-bridge-layer`.

## Package import convention

The canonical import namespace is `sbl`, e.g.:

- `sbl.core.bridge`
- `sbl.adapters.base`
- `sbl.examples.basic_usage`
